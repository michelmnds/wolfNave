/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */
import "./style.css";

export const PTModal = ({ id, name, image, specialities, location }) => {
  return (
    <div className="ptModalContainer">
      <div className="ptModal"></div>
    </div>
  );
};
